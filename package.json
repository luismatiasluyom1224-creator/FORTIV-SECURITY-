{
  "name": "krav-security-app",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}